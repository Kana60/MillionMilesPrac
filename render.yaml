services:
  - type: web
    name: encar-catalog
    runtime: docker
    plan: free
    autoDeploy: true
    healthCheckPath: /

  - type: cron
    name: encar-daily-update
    runtime: docker
    plan: free
    schedule: "0 3 * * *"
    dockerCommand: "python -m app.cron_update"
